[AccessTransformers]
; No engine-internal access needed at the scaffold stage.
;
; Add Friend=(Class="<FGClass>", FriendClass="<YourClass>") entries here as the
; weapon / subsystem needs to reach protected/private FactoryGame members (e.g.
; weapon firing internals, equipment item-state). Re-sync this Config folder to
; the SatisfactoryModLoader\Mods\LaserRifleMod tree whenever it changes
; (the build script does this automatically).
